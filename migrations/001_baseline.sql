-- Migration 001 — Baseline
-- Marca o ponto de partida do sistema de migrações.
-- O schema existente (empresas, usuarios, produtos, vendas, compras,
-- estoque, financeiro, etc.) foi criado via initDb() e já está em produção.
-- Esta migration não altera nada — apenas registra o baseline.
SELECT 1;
